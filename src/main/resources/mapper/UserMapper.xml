<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ZXKC.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.ZXKC.domain.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="nickname" property="nickname"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_info(username, password, phone, nickname)
        VALUES (#{username}, #{password}, #{phone}, #{nickname})
    </insert>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT id, username, password, phone, nickname
        FROM user_info
        WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, username, password, phone, nickname
        FROM user_info
        WHERE id = #{id}
    </select>
</mapper>
